<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

// If using a class name other than the the name of the table that is in your database, then do the following
    /**
     * Example: 
     * class PostAdmin extends Model {
     * 
     *  // define the name of the actual column name in the database table:
     * example 1: protected $table = 'posts' (posts is the name of the table in the database)
     * example 2: protected $primaryKey = 'post_id' (this is defined in the case the primary key is named 'id')
     * }
     * 
     * 
     */

class Post extends Model
{

    
    
}
